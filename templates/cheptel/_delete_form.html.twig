<form method="post" action="{{ path('app_cheptel_delete', {'id': cheptel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cheptel.id) }}">
    <button class="btn">Delete</button>
</form>
