<form method="post" action="{{ path('app_groupe_traitement_delete', {'id': groupe_traitement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupe_traitement.id) }}">
    <button class="btn">Delete</button>
</form>
